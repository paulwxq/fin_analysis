现在我有一个需求，需要从 /mnt/d/BaiduNetdiskDownload/A股分时数据/A股_分时数据_沪深/1分钟_前复权_按年汇总 目录下加载数据到 Postgesql-18+timescaledb 数据库，请你写一份这个模块的设计文档，我的具体要求如下：
1.访问 /mnt/d/BaiduNetdiskDownload/A股分时数据/A股_分时数据_沪深/1分钟_前复权_按年汇总 目录下的文件，分析文件的结构，生产数据库建表语句，请注意当前数据库的名字是fin_db，已经启用了timescaledb扩展。
2.你可以访问 /mnt/d/BaiduNetdiskDownload/复权因子 目录下的代码，这些代码可以帮助你了解文件的结构，帮助你生成建表语句，另外也许能帮助你了解计算方式。
3./mnt/d/BaiduNetdiskDownload/A股分时数据/A股_分时数据_沪深/1分钟_前复权_按年汇总 目录下的的文件都是zip压缩包，你可以在加载到数据库的时候，把它们解压，或者直接读取。
4.目标数据库的访问信息：
psql -h 172.29.128.1 -U postgres -d fin_db
密码是：PostgreSQL-18
5.在当前的wsl环境下，已经安装了psql客户端，你可以调用它访问数据库。
6.当前项目根目录下有一个.venv的虚拟环境，我们使用uv管理依赖，如果对于当前项目需要的依赖，你可以写到文档中，我会手工执行它们。
7.当前模块为数据加载模块，当前模块的代码都放在项目一个子目录下，比如data_infra，或者你起一个更好的名字。
8.数据库的连接信息，你可以放在项目根目录下的.env文件中，对于加载的其他参数，比如并行度等等，这些请放到config.py文件中。
现在请你根据上面的需求，设计数据加载模块，包括模块的文件目录结构，依赖的包，目标表的表结构等等。再次提醒，当前数据库是postgresql v18 + timescaledb，我们后续需要访问这个数据库表，使用当前的数据生成月K线数据，所以我们才使用timescaledb作为时序数据库，以提高查询效率。
